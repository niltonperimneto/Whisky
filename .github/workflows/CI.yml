name: CI

on:
  # Docs-only changes can't affect the Swift build or tests, so skip the whole
  # workflow. Actions minutes are free on public repos, but macOS runner
  # concurrency is limited — skipped jobs are queue time given back to real runs.
  # paths-ignore skips a run only when *every* changed file matches, so a PR that
  # also touches code still runs the full suite.
  #
  # NOTE: `main` has no required status checks today, so a skipped docs PR simply
  # reports no checks and stays mergeable. If branch protection is ever added that
  # *requires* these checks by name, note that the ui-test job is also gated by
  # the `changes` job below and a skipped run reports no status — required-check
  # names would need a carve-out for it.
  push:
    branches: [main, preview]
    paths-ignore: &docs-paths
      - '**.md'
      - 'docs/**'
      - 'images/**'
      - 'LICENSE'
  pull_request:
    branches: [main, preview]
    paths-ignore: *docs-paths
  workflow_dispatch:

permissions:
  contents: read

# Shared environment variables for consistency across jobs
env:
  XCODE_VERSION: 'latest-stable'

# Prevent duplicate runs on the same PR
concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  # Lint job runs first on cheaper ubuntu runner
  lint:
    name: SwiftLint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: SwiftLint
        uses: norio-nomura/action-swiftlint@3.2.1
        with:
          args: --strict

  # Format check job runs in parallel with lint
  format:
    name: SwiftFormat
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Install SwiftFormat
        run: |
          # Install SwiftFormat via pre-built binary
          wget -q https://github.com/nicklockwood/SwiftFormat/releases/download/0.58.7/swiftformat_linux.zip
          unzip -q swiftformat_linux.zip
          chmod +x swiftformat_linux
          sudo mv swiftformat_linux /usr/local/bin/swiftformat
      - name: Check Formatting
        run: |
          swiftformat --lint . || {
            echo ""
            echo "❌ Code formatting issues found!"
            echo "Run 'swiftformat .' locally to fix formatting."
            exit 1
          }

  # A partial en-GB is worse than none: macOS picks one localization per process
  # and renders the raw key for anything missing from it, so a single string
  # added without regenerating shows `config.audio.driver.auto` to every British
  # user rather than falling back to en.
  localization:
    name: Localization
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Check en-GB covers every string
        run: |
          ./scripts/british-english.py --check || {
            echo ""
            echo "Run ./scripts/british-english.py locally and commit the result."
            exit 1
          }
      # The same trap in every other language: translations land per release,
      # strings are added between them, and a German user reads
      # `library.card.settings` rather than English.
      - name: Check every locale carries every string
        run: |
          ./scripts/locale-coverage.py --check || {
            echo ""
            echo "Run ./scripts/locale-coverage.py locally and commit the result."
            exit 1
          }
      # The other two ways a key renders as its own raw text: an entry with
      # translations but no English source, and a key used in code that was
      # never added to the catalog. Both have shipped before.
      - name: Check every key has an English source and a catalog entry
        run: ./scripts/check-strings.py

  # Detects which areas a PR touches so macOS jobs can be skipped when they
  # can't be affected. Runs on every event; the outputs are only consulted for
  # pull requests (pushes to main always run everything).
  changes:
    name: Detect changed areas
    runs-on: ubuntu-latest
    outputs:
      app: ${{ steps.filter.outputs.app }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            app:
              - 'Whisky/**'
              - 'WhiskyUITests/**'
              - 'Whisky.xcodeproj/**'
              - '.github/workflows/CI.yml'

  # All three schemes build in one job: they share WhiskyKit and most compiled
  # sources through a common DerivedData, so sequential scheme builds are mostly
  # incremental — one checkout/Xcode/cache setup instead of three matrix jobs.
  build:
    name: Build all schemes
    needs: [lint, format]
    # macos-26: the app's deployment target is 26.0, so an older runner cannot
    # launch it, and the UI tests fail before they start.
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ env.XCODE_VERSION }}

      # Cache includes WhiskyKit/.build for consistency with test job cache paths
      # (swift build creates artifacts there, xcodebuild uses DerivedData)
      - name: Cache SPM Dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/Library/Developer/Xcode/DerivedData/*/SourcePackages
            .build
            WhiskyKit/.build
          key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-spm-

      # SwiftLint build phase automatically skips when CI=true (set by GitHub Actions)
      - name: Build
        run: |
          for scheme in Whisky WhiskyCmd WhiskyThumbnail; do
            echo "::group::Build $scheme"
            xcodebuild -project Whisky.xcodeproj \
              -scheme "$scheme" \
              -configuration Debug \
              build \
              CODE_SIGN_IDENTITY="" \
              CODE_SIGNING_REQUIRED=NO \
              CODE_SIGNING_ALLOWED=NO
            echo "::endgroup::"
          done

      # Guards against the class of regression fixed in #134: a resource that the
      # app expects at runtime silently not making it into the bundle. winetricks
      # and its verb catalog are shipped in Contents/Resources (like cabextract);
      # if the "Embed WhiskyCmd" copy phase drops them, dependency installs break
      # at runtime but the build stays green — so assert on the built artifact.
      - name: Verify bundled app resources
        run: |
          # Ask for the exact product path rather than globbing: this fork's
          # PRODUCT_NAME is "Whisky Preview", and a glob loose enough to catch that
          # also catches WhiskyUITests-Runner.app.
          app=$(xcodebuild -project Whisky.xcodeproj -scheme Whisky -configuration Debug \
            -showBuildSettings 2>/dev/null \
            | awk -F' = ' '
                /^ *BUILT_PRODUCTS_DIR = /  { dir = $2 }
                /^ *FULL_PRODUCT_NAME = /   { name = $2 }
                END { if (dir != "" && name != "") print dir "/" name }')
          if [ -z "$app" ] || [ ! -d "$app" ]; then
            echo "::error::app bundle not found after build (resolved to '${app:-<empty>}')"
            exit 1
          fi
          echo "Checking $app"
          missing=0
          for res in winetricks verbs.txt cabextract; do
            if [ ! -f "$app/Contents/Resources/$res" ]; then
              echo "::error::missing bundled resource: Contents/Resources/$res"
              missing=1
            fi
          done
          [ "$missing" -eq 0 ] || exit 1
          echo "Bundled resources present: winetricks, verbs.txt, cabextract"

  # XCUITest job - runs WhiskyUITests against the Whisky app target.
  # Tests that need bottle fixtures call `try requireBottleFixture()` which
  # XCTSkips them when the user container is empty (always true on a fresh
  # CI runner). The fixture-free tests - create-bottle sheet flow and the
  # toolbar checks - run for real and verify the app launches without
  # crashing and the toolbar surface renders.
  #
  # No user-controllable inputs are interpolated into shell commands here;
  # all values are constants from the workflow file or static action inputs.
  #
  # Skipped on PRs that don't touch the app target, the UI tests, or the
  # project file: WhiskyKit-only PRs are covered by the unit-test job and the
  # app still compiles in `build`. Pushes to main always run the full suite,
  # so a Kit change that alters UI behavior is still caught post-merge.
  ui-test:
    name: WhiskyUITests
    needs: [lint, format, changes]
    if: github.event_name != 'pull_request' || needs.changes.outputs.app == 'true'
    # macos-26: the app's deployment target is 26.0, so an older runner cannot
    # launch it, and the UI tests fail before they start.
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ env.XCODE_VERSION }}

      - name: Cache SPM Dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/Library/Developer/Xcode/DerivedData/*/SourcePackages
            .build
            WhiskyKit/.build
          key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-spm-

      - name: Run WhiskyUITests
        run: |
          rm -rf TestResults.xcresult
          # Retry failed tests up to 3 total runs. XCUITests against a SwiftUI
          # surface are timing-sensitive (e.g. waiting for the create-bottle
          # sheet to render on a busy CI runner), so a single flake shouldn't
          # red-X main. A genuinely broken test still fails after all retries.
          xcodebuild -project Whisky.xcodeproj \
            -scheme Whisky \
            -configuration Debug \
            -destination 'platform=macOS' \
            -only-testing:WhiskyUITests \
            -enableCodeCoverage YES \
            -resultBundlePath TestResults.xcresult \
            -retry-tests-on-failure \
            -test-iterations 3 \
            test \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      # App-target (Whisky scheme) coverage from the UI-test run, uploaded under the
      # `whiskyapp` Codecov flag so the SwiftUI app/CLI/thumbnail aren't completely
      # dark. This is BEST-EFFORT: every step below is `continue-on-error`, so a broken
      # extraction just means no app-target number that run and can never gate CI.
      - name: Convert app-target coverage to Cobertura
        if: always()
        continue-on-error: true
        run: |
          brew install xcresultparser
          xcresultparser --output-format cobertura TestResults.xcresult > coverage-app.xml
          # Normalize any absolute source paths to repo-relative so Codecov's `Whisky/**`
          # flag filter matches. No-op if xcresultparser already emits relative paths.
          sed -i '' "s|${GITHUB_WORKSPACE}/||g" coverage-app.xml || true
          echo "=== coverage-app.xml (first 20 lines) ==="
          head -20 coverage-app.xml || true

      - name: Upload app-target coverage to Codecov
        if: always()
        continue-on-error: true
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage-app.xml
          flags: whiskyapp
          fail_ci_if_error: false
          verbose: true

      - name: Upload xcresult bundle on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: ui-test-xcresult
          path: TestResults.xcresult
          if-no-files-found: ignore

  # Test job for WhiskyKit - always runs to ensure coverage is uploaded
  test:
    name: Run Tests
    needs: [lint, format]
    # macos-26: the app's deployment target is 26.0, so an older runner cannot
    # launch it, and the UI tests fail before they start.
    runs-on: macos-26
    
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      
      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ env.XCODE_VERSION }}
      
      # Cache includes WhiskyKit/.build because swift build/test creates
      # build artifacts there (unlike xcodebuild which uses DerivedData)
      - name: Cache SPM Dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/Library/Developer/Xcode/DerivedData/*/SourcePackages
            .build
            WhiskyKit/.build
          key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-spm-
      
      - name: Show Swift version
        run: swift --version
      
      - name: Build WhiskyKit
        run: swift build --package-path WhiskyKit
      
      - name: Run Tests with Code Coverage
        run: swift test --package-path WhiskyKit --enable-code-coverage
          
      - name: Generate Coverage Report
        id: coverage
        run: |
          cd WhiskyKit

          # Debug: Show build directory structure
          echo "=== Build directory structure ==="
          find .build -type d -name "debug" 2>/dev/null | head -5

          echo "=== Looking for xctest bundles ==="
          find .build -name "*.xctest" -type d 2>/dev/null

          echo "=== Looking for profdata files ==="
          find .build -name "*.profdata" 2>/dev/null

          # Find the test binary - search entire .build directory for architecture-specific paths
          TEST_BINARY=""

          # Check for SPM test binary in xctest bundle (searches all architectures)
          XCTEST_BUNDLE=$(find .build -path "*debug*" -name "WhiskyKitPackageTests.xctest" -type d 2>/dev/null | head -1)
          if [ -n "$XCTEST_BUNDLE" ]; then
            BUNDLE_NAME=$(basename "$XCTEST_BUNDLE" .xctest)
            TEST_BINARY="$XCTEST_BUNDLE/Contents/MacOS/$BUNDLE_NAME"
            echo "=== Found xctest bundle: $XCTEST_BUNDLE ==="
            echo "=== Test binary path: $TEST_BINARY ==="
          fi

          # Verify the binary exists
          if [ ! -f "$TEST_BINARY" ]; then
            echo "=== Binary not in bundle, searching for standalone executable ==="
            TEST_BINARY=$(find .build -path "*debug*" -type f -name "WhiskyKitPackageTests" -perm +111 2>/dev/null | head -1)
            echo "=== Found: $TEST_BINARY ==="
          fi

          if [ -f "$TEST_BINARY" ]; then
            echo "=== Using test binary: $TEST_BINARY ==="

            PROFDATA=$(find .build -name "default.profdata" 2>/dev/null | head -1)
            if [ -n "$PROFDATA" ]; then
              echo "=== Using profdata: $PROFDATA ==="

              # Get the absolute path to WhiskyKit for path mapping
              WHISKYKIT_PATH="$(pwd)"
              echo "=== WhiskyKit path: $WHISKYKIT_PATH ==="

              # Generate coverage, filtering out build artifacts and dependencies
              xcrun llvm-cov export \
                "$TEST_BINARY" \
                -instr-profile="$PROFDATA" \
                -format=lcov \
                --ignore-filename-regex='\.build/' > coverage.raw.lcov

              # Convert absolute paths to relative paths for Codecov
              # Maps /Users/runner/work/Whisky/Whisky/WhiskyKit/Sources/... -> WhiskyKit/Sources/...
              sed "s|${WHISKYKIT_PATH}/|WhiskyKit/|g" coverage.raw.lcov > coverage.lcov
              rm coverage.raw.lcov

              # Verify the coverage file was created and has content
              if [ -s coverage.lcov ]; then
                echo "✅ Coverage report generated successfully"
                echo "=== Coverage file size: $(wc -c < coverage.lcov) bytes ==="
                echo "=== First 30 lines of coverage.lcov ==="
                head -30 coverage.lcov
                echo "=== Unique source file paths ==="
                grep "^SF:" coverage.lcov | head -10
                echo "has_coverage=true" >> $GITHUB_OUTPUT
              else
                echo "❌ Coverage file is empty"
              fi
            else
              echo "❌ Warning: No profdata file found"
            fi
          else
            echo "❌ Warning: Could not find test binary"
            echo "=== Listing all executables in .build ==="
            find .build -path "*debug*" -type f -perm +111 2>/dev/null | grep -i test | head -20
          fi
      
      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: WhiskyKit/coverage.lcov
          flags: whiskykit
          fail_ci_if_error: false
          verbose: true
      
      - name: Upload Coverage Report (Artifact)
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-report
          path: WhiskyKit/coverage.lcov
          if-no-files-found: ignore
