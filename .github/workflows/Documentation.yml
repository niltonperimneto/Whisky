name: Documentation

# Publishes from `preview`, the branch this fork releases from. `main` tracks
# upstream and must not overwrite our Pages site (it serves WhiskyWineVersion.plist,
# which the app reads to find the runtime).
on:
  push:
    branches: [preview]
    paths:
      - 'WhiskyKit/**'
      - 'dist/pages/**'
      - '.github/workflows/Documentation.yml'
  workflow_dispatch:

# Sets permissions for GitHub Pages deployment
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: pages
  cancel-in-progress: false

env:
  XCODE_VERSION: 'latest-stable'

jobs:
  build-docs:
    name: Build Documentation
    # macos-26: the app's deployment target is 26.0, so an older runner cannot
    # launch it, and the UI tests fail before they start.
    runs-on: macos-26
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ env.XCODE_VERSION }}

      - name: Cache SPM Dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/Library/Developer/Xcode/DerivedData/*/SourcePackages
            .build
            WhiskyKit/.build
          key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-spm-

      - name: Build Documentation
        run: |
          cd WhiskyKit
          swift package --allow-writing-to-directory ../docs \
            generate-documentation \
            --target WhiskyKit \
            --disable-indexing \
            --transform-for-static-hosting \
            --hosting-base-path Whisky \
            --output-path ../docs

      - name: Stage distribution files
        run: |
          if [ -d dist/pages ]; then
            cp -R dist/pages/. docs/
          fi
          ls -la docs/

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: docs

  deploy:
    name: Deploy to GitHub Pages
    needs: build-docs
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
